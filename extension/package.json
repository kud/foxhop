{
  "name": "foxhop-extension",
  "version": "1.0.1",
  "description": "Firefox extension that focuses a tab by URL on request from the foxhop native host.",
  "private": true,
  "author": "kud",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "web-ext run --firefox=nightly",
    "build": "rm -rf web-ext-artifacts/* 2>/dev/null || true && web-ext build --source-dir=. --artifacts-dir=web-ext-artifacts --ignore-files=publish.sh",
    "lint": "web-ext lint --ignore-files=publish.sh",
    "sign": "web-ext sign --channel=unlisted",
    "publish": "bash publish.sh"
  },
  "devDependencies": {
    "web-ext": "10.4.0"
  }
}
